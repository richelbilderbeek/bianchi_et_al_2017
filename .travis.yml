language: r
dist: bionic
cache: packages

r_github_packages:
  - jtextor/epitope-prediction

script:
  - echo "Prerequisites"
  - make --version
  - wget --version
  - echo "Real work"
  - make

after_success:
  - find . | egrep -v "\.git"


after_failure:
  - find . | egrep -v "\.git"

